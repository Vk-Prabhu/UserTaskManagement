<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background: hsla(191, 60%, 89%, 1);
            background: linear-gradient(90deg, hsla(191, 60%, 89%, 1) 0%, hsla(56, 59%, 86%, 1) 50%, hsla(25, 85%, 69%, 1) 100%);
            background: -moz-linear-gradient(90deg, hsla(191, 60%, 89%, 1) 0%, hsla(56, 59%, 86%, 1) 50%, hsla(25, 85%, 69%, 1) 100%);
            background: -webkit-linear-gradient(90deg, hsla(191, 60%, 89%, 1) 0%, hsla(56, 59%, 86%, 1) 50%, hsla(25, 85%, 69%, 1) 100%);
            filter: progid: DXImageTransform.Microsoft.gradient( startColorstr="#D3EEF4", endColorstr="#F1EEC8", GradientType=1 );
        }

        .dashboard-header {
            text-align: center;
            margin-top: 50px;
            margin-bottom: 30px;
            font-size: 36px;
            font-weight: bold;
            color: #343a40;
        }

        .dashboard-container {
            width: 100%;
            max-width: 800px;
            margin: 0 auto;
            text-align: center;
        }

            .dashboard-container a {
                font-size: 20px;
                color: #007bff;
                text-decoration: none;
                transition: color 0.3s;
            }

                .dashboard-container a:hover {
                    color: #0056b3;
                }
    </style>
</head>
<body>

    <h1 class="dashboard-header">
        Welcome to the Dashboard
    </h1>

    <div class="container-fluid dashboard-container">
        <p>
            <a class="navbar-brand" href="@Url.Action("TaskList","TaskDetail")">
                Task Management
            </a>
            <a class="navbar-brand" href="@Url.Action("Logout","Account")">
                Logout
            </a>
        </p>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
